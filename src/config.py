camera_types: list = [
    "LOCAL_CAMERA_PORT",
    "WLAN_CAMERA_IP",
    "LOCAL_VIDEO",
    "LOCAL_IMAGE"
]

# The camera config has to be the camera port and the camera type
CAMERA_CONFIG: tuple = (0, camera_types[0])

